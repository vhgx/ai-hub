def simple_ai_model(input_value: float) -> float:
    """
    Simula um modelo de IA previamente treinado.
    Exemplo simples: multiplicação ponderada.
    """
    weight = 2.5
    return input_value * weight